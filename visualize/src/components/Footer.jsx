// src/components/Footer.jsx
// eslint-disable-next-line no-unused-vars
import React from 'react';

const Footer = () => {
    return (
        <footer>
            {/* Footer content */}
        </footer>
    );
};

export default Footer;  // Make sure the default export is used
